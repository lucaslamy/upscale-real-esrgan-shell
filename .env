# Path to the Real-ESRGAN NCNN Vulkan binary
REALESRGAN_BIN=/opt/realesrgan/realesrgan-ncnn-vulkan

# Working directory used during upscaling
WORKDIR=/tmp/upscale

# Subdirectories for extracted and upscaled frames
FRAMES_DIR=$WORKDIR/frames
UPSCALED_DIR=$WORKDIR/frames_upscaled

# Log file to store upscaling history
HISTORY_LOG=$HOME/.upscale_history.log

# Input and output folders for CLI mode
INPUT_DIR=$HOME/upscale/input
OUTPUT_DIR=$HOME/upscale/output
